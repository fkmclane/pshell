#!/bin/sh
if [ "$HOSTNAME" == "login001.palmetto.clemson.edu" ]; then
	exec /opt/pbs/default/bin/"$(basename "$0")" "$@"
else
	exec socat - unix:/tmp/"$PBS_JOBID".sock <<EOF
cd "$(pwd)"
"$0"$(for arg in "$@"; do echo -n " \"$arg\""; done)
EOF
fi
